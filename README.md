# Exercise: Break the build!

For this exercise:

- create a fork of this repository
- add some changes to the `index.js` so that the build doesn't pass.
- create a pull request against `master` branch in this repo from your fork
